# Administrative schema jobs use this config while the game process is stopped.
mode = "game"

[persistence]
auto_sync_schema = false

[persistence.realm]
connection_string = "$MOONGATE_REALM_SCHEMA_DATABASE"
