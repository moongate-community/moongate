namespace Moongate.Ultima.Types;

/// <summary>
///     The race of a character, valued as the client sends it at character creation.
/// </summary>
public enum RaceType : byte
{
    Human = 0,
    Elf = 1,
    Gargoyle = 2
}
