# Moongate.Server.Admin

Embedded optional gRPC administration plugin distributed with Moongate Server. It hosts the versioned administration contracts using existing account services and host-owned session infrastructure. It is not a disk plugin or a standalone NuGet package.
