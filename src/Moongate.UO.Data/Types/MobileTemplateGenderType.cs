namespace Moongate.UO.Data.Types;

/// <summary>How a mobile template chooses a spawned mobile's gender: a fixed value or a per-spawn roll.</summary>
public enum MobileTemplateGenderType
{
    Male = 0,
    Female = 1,
    Random = 2
}
