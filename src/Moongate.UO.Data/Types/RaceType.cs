namespace Moongate.UO.Data.Types;

/// <summary>Playable races, valued by the UO race index used in the client protocol.</summary>
public enum RaceType : byte
{
    Human = 0,
    Elf = 1,
    Gargoyle = 2
}
