# Moongate v2 - Current Status (2026-03-02)

## Summary
- README and DocFX docs are now aligned on protocol/runtime coverage.
- UO feature support is documented by real wired opcodes (inbound/outbound) and active `0xBF` subcommands.
- Obsidian vault navigation has been updated with this status snapshot and a matching journal entry.

## Highlights Since 2026-02-23

### Documentation Alignment
- Updated `README.md` section **UO Feature Support (Current)** to reflect actual runtime handlers and active packet flow.
- Updated `docs/articles/networking/protocol.md` with:
  - current inbound opcode coverage,
  - wired `0xBF` subcommands (`0x06`, `0x1A`, `0x2C`, `0x2D`, `0x2E`),
  - active outbound gameplay packet groups.
- Updated `docs/articles/getting-started/introduction.md` so implemented/planned feature lists match current server behavior.

### Obsidian Vault Consistency
- Added this status file under `docs/plans/`.
- Added journal note `docs/journal/2026-03-02.md`.
- Updated `docs/Home.md` links to include the new status and journal note.

## Verification Snapshot
- `dotnet test --no-build`: passed (`680` tests).

## Near-Term Open Items
- Keep `docs/articles/networking/packets.md` and protocol docs synchronized whenever new runtime handlers are added.
- Add a lightweight “last verified protocol coverage” note in docs to reduce drift over time.
