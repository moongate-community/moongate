# Troubleshooting

When no Ultima Online directory is supplied on the command line, the server resolves `~/uo` and assigns it to the loaded configuration; a supplied command-line directory overrides that default. For connection diagnosis, the current network configuration separates the local listener address and port from the public address advertised to clients.

Review [Operations](./operations.md), or return to the [Server Guide](./index.md).
