# Configuration

The server loads the `moongate` YAML configuration section from its root directory. Current settings cover the shard name, Ultima Online data directory, local listener address and port, and the public address advertised to clients; command-line values can select the root and Ultima Online directories.

Review [Installation](./installation.md), or continue to [Operations](./operations.md).
